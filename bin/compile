#!/usr/bin/env bash

set -eo pipefail

# Buildpack Steps.
puts-step() {
  echo "-----> $*"
}

BUILD_DIR=$1

puts-step "$ python manage.py compress"
python $BUILD_DIR/manage.py compress --traceback 2>&1
